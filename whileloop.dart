//excutes acondition until its false
void main() {
  var a = 1;
  var maxnum = 10;
  while (a < maxnum) {
    print(a); // prints untils its faule
    a = a + 1; //increase valuse of a after very iteration /loop/cycle
  }
}
